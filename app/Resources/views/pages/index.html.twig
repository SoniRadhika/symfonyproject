
{{ include('pages/include/header.html.twig')}}
<div class="container"><br>

<a href="{{ absolute_url(asset(''))}}app_dev.php/create" class="btn btn-warning">Create Post</a>
<br></br>
<h3>View Posts</h3>
 
<table class="table table-hover">
  <thead>
    <tr>
      <th scope="col">Id</th>
      <th scope="col">Title</th>
      <th scope="col">Description</th>
      <th scope="col">Category</th>
      <th scope="col">Action</th>
    </tr>
  </thead>
  <tbody>
   
    <tr>
    {% for post in posts%}
      <th scope="row">{{ post.id }}</th>
      <td>{{ post.title }}</td>
      <td>{{ post.description }}</td>
      <td>{{ post.category }}</td>
      <td>
<a href="{{ absolute_url(asset(''))}}app_dev.php/view/{{post.id}}" class="btn btn-info btn-sm">View</a>
<a href="{{ absolute_url(asset(''))}}app_dev.php/edit/{{post.id}}" class="btn btn-success btn-sm">Edit</a>
     <a href="{{ absolute_url(asset(''))}}app_dev.php/delete/{{post.id}}" class="btn btn-primary btn-sm">Delete</a></td>
    </tr>
   {% endfor %}
  </tbody>
</table>

</div>
{{ include('pages/include/footer.html.twig')}}